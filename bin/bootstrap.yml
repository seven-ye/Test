spring:
  application:
    name: qmjj-other-service    # 项目名称尽量用小写
eureka:
  client:
    serviceUrl:
      #defaultZone: http://192.168.0.2:8761/eureka/    # 指定注册中心的地址
      defaultZone: http://localhost:8761/eureka/    # 指定注册中心的地址
  instance:
    preferIpAddress: true